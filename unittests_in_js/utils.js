// utils.js
const utils = {
    calculateNumber: (type a, b) => {
        if (type === 'SUM') {
            return a + b;
        }
        // If type is not 'SUM', then it is 'SUBTRACT'
}

module.exports = utils; 